let expect = require('chai').expect;
let Rpc = require('./src');

// TODO write tests for rpc class

describe('Rpc', function(){
  it('does bar', function(){
    expect('this').to.not.equal('that');
  });
});
